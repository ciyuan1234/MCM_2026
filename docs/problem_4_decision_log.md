# 问题 4 决策日志

- 版本日期：2026-09-12
- 状态：模型文档待用户确认；尚未实现

| 编号 | 决策问题 | 选项 | 选择 | 原因与证据 | 影响 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| Q4-001 | 几何处理 | 固定半径 / 仿射收缩 / 非均匀收缩 | 仿射收缩 `r=ξR(t)` | 附件 2 只给外表轮廓；仿射是唯一可识别假设；非均匀变形不可辨识 | 控制方程改写为材料坐标形式 | 待确认 |
| Q4-002 | 移动边界方程写法 | 材料坐标（无对流项）/ 欧拉形式（含对流项） | 材料坐标、不含对流项 | 干基含水率与温度是材料量；材料点相对网格静止；含对流项需要 `Ṙ` 的数值微分，且会把体积压缩误作水分富集（G-037） | 方程与问题 2、3 同形，只多度量因子 | 已确认（用户） |
| Q4-003 | 物性公式 | 附录 3 / 附录 4 | 附录 4 | 题目要求问题 4 采用收缩态经验公式；已用 PDF 数值核对 | 密度、比热、导热、扩散系数全部改变 | 已确认 |
| Q4-004 | 表面系数 `h`、`hm` | 附录 2 / 重新拟合 | 附录 2 | 附录 4 未重新给出边界系数，沿用是最小假设（与 G-020 同口径） | 边界条件不变 | 待确认 |
| Q4-005 | 半径数据处理 | 平滑/拟合 / 直接分段线性 | 直接分段线性，端点外保持末值 | 遵守"只校验、不改值"；`R(t)` 在材料坐标形式下不需要导数，无须平滑 | 半径序列原样使用 | 待确认 |
| Q4-006 | `result4.xlsx` 列含义 | 材料点初始距离 / 当前欧氏距离 / 归一化位置 | 材料点初始距离，末列"药材表面" | 半径降到 1.198 cm 后，当前欧氏距离列会大面积无定义；材料点标注与表 6 一致（G-038） | 输出 20 列材料点 + 1 列表面 | 已确认（用户） |
| Q4-007 | 达标判据 | 圆心 / 全域最大 / 平均 | 圆心（材料坐标 `ξ=0`） | 与问题 3 同口径；需数值证明圆心是空间最大值 | 需测试锁定 | 待确认 |
| Q4-008 | 空间与时间步 | 沿用问题 3 / 重新确定 | 初值 `dr=0.03125 mm`、`dt=15 s`，以收敛复算为准 | 问题 3 已证明粗网格在近表面带会超阈值 | 需专门的收敛复算 | 待确认 |
| Q4-009 | 求解上限 | 72 h / 更长 | 72 h（附件 2 覆盖范围） | 量级估计 `t_f ≈ 40～60 h`；超过上限则报错并请示，不外推 | 未达标时报错终止 | 待确认 |
| Q4-010 | 是否启动并行实验 | 不启动 / 启动 | 启动 EXP-003（半径处理敏感性） | 半径处理方式会改变路径长度，是唯一会显著改变结论的竞争假设 | 需先批准任务卡 | 待批准 |
| Q4-011 | 干物质守恒不一致 | 调整物性 / 调整半径 / 原样使用并报告 | 原样使用并报告 | 附录 4 隐含干密度增大 `2.41×`，收缩要求 `2.79×`，相差约 13.5%；题目两式均为给定事实，不得擅自改动 | 写入论文局限 | 待确认 |

## 实现记录

尚未实现。用户确认 `docs/problem_4_model.md` 后按第 16 节执行。

## 验证记录

尚未生成。

## 数据处理说明

沿用 `docs/data_processing.md`：附件只读、只校验不改值；半径与空气边界均用分段线性插值、端点外保持末值；新增半径单调性与正性检查（违反则报错终止）。

## 风险与待确认

1. 是否接受仿射收缩假设；
2. 是否接受"材料坐标、不含对流项"的方程写法（已由你确认，此处留档）；
3. 是否接受材料点列的 `result4.xlsx` 口径（已由你确认，此处留档）；
4. 是否批准 EXP-003 半径处理敏感性实验；
5. 若 `t_f` 超过 72 h，如何延长半径序列。
