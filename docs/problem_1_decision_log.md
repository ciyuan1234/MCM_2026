# 问题 1 决策日志

- 版本日期：2026-09-11
- 状态：问题 1 基准模型已实现并验证；等待用户确认结果

| 编号 | 决策问题 | 选项 | 选择 | 原因与证据 | 影响 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| Q1-001 | 空间维度 | 三维 / 二维 / 一维径向 | 一维径向 | 长 25 cm、半径 2 cm，端部面积相对很小；先按题目阶段做最小模型 | 降低计算复杂度 | 待问题 1 模型确认 |
| Q1-002 | 物性处理 | 常数 / 变物性 | 常数 | 附录 2 明确给出问题 1 的常数参数，预热阶段仅 30 min | 问题 1 使用固定参数 | 待问题 1 模型确认 |
| Q1-003 | 初始条件 | 使用附件 1 空气状态 / 使用题目药材初始状态 | 使用题目药材初始状态 | 题目明确药材 `T=28 °C`、`C=2.55 kg/kg`；附件 1 是空气状态 | 初始场与边界驱动分开 | 已核对 |
| Q1-004 | 空气边界插值 | 最近邻 / 阶梯 / 分段线性 | 分段线性 | 保持一阶连续趋势，是不引入额外物理假设的最小处理 | 边界值随 t 变化 | 待确认 |
| Q1-005 | 中心边界 | 直接代入 `1/r` / 单独对称控制体 | 单独对称控制体 | 避免 `r=0` 奇异性，符合圆柱对称物理条件 | 中心节点需要专门离散 | 已确认 |
| Q1-006 | 时间推进 | 显式 / 隐式 | 隐式 Euler | 用户确认的 scipy 技术路线；适合后续变系数和长时程 | 每步需要非线性处理 | 已确认 |
| Q1-007 | 空间步长 | 0.1 cm / 更粗 | 0.1 cm | 与题目要求的输出距离网格一致 | 结果可直接映射到模板 | 待问题 1 模型确认 |
| Q1-008 | 时间步长 | 1 s / 更粗 | 1 s | 与 result1 的完整输出间隔一致 | 需要 1800 个时间步 | 待问题 1 模型确认 |
| Q1-009 | 蒸发潜热 | 加入 / 不加入 | 暂不加入 | 题目未给潜热参数，不能虚构 | 作为简化假设并在论文中说明 | 待确认 |
| Q1-010 | 模板处理 | 原地修改 / 复制并扩展 | 复制并扩展 | 附件只读，模板只有示意行列 | 输出文件放在 `outputs/` | 已确认 |
| Q1-011 | 数据清洗 | 删除/平滑/保留 | 只校验，不改值 | 无缺失、重复和局部异常；平滑会改变原始边界驱动 | 原始附件保持只读 | 已确认 |
| Q1-012 | 边界插值 | 前值保持/后值保持/区间平均/分段线性 | 分段线性 | 最小额外假设，保持趋势且通过敏感性检查 | 当前结果绑定该规则 | 已确认 |
| Q1-013 | 处理变更 | 沿用旧结果/重新生成 | 重新生成 | 前值保持与线性插值在表面温度上约差 0.18 °C | 任何处理方式改变都使旧结果失效 | 已确认 |

## 实现与验证记录

实现入口：

```bash
.venv/bin/python analysis/solve.py --problem 1
```

测试：

```bash
.venv/bin/python -m pytest -q
```

结果：当前 11 项测试全部通过，其中包含 4 项附件数据完整性和插值测试。

守恒检查：

| 项目 | 内部变化 | 边界累计通量 | 相对误差 |
| --- | ---: | ---: | ---: |
| 热量 | 19211.524993688992 | 19211.524993660085 | 1.5047e-12 |
| 水分 | -0.00032401962815486736 | -0.0003240196281548634 | 1.2213e-14 |

收敛检查（在 `t=1800 s`、`r=0, 1, 2 cm` 上比较）：

| 对比 | 温度最大相对变化 | 水分浓度最大相对变化 |
| --- | ---: | ---: |
| `dr=2 mm` 对 `dr=1 mm` | 2.2765e-5 | 3.7053e-3 |
| `dr=0.5 mm` 对 `dr=1 mm` | 5.5791e-6 | 7.1272e-4 |
| `dt=2 s` 对 `dt=1 s` | 3.5800e-5 | 5.9204e-5 |

结果文件：

- `outputs/result1.xlsx`：两个工作表均为 1802 行、22 列（含表头）。
- `outputs/result1_solution.json`：完整数值结果。
- `outputs/run_manifest.json`：输入、参数、代码哈希、命令和环境。

## 数据处理敏感性

在 `t=1800 s` 上，以分段线性插值为基准：

| 处理方式 | 最大温度差 / °C | 最大水分浓度差 | 结论 |
| --- | ---: | ---: | --- |
| 前值保持 | 0.1803 | 9.76e-5 | 有可见差异，趋势不变 |
| 后值保持 | 0.1661 | 9.13e-5 | 有可见差异，趋势不变 |
| 区间中点 | 0.0029 | 1.60e-6 | 很小 |
| 4 分钟移动平均 | 0.0108 | 2.79e-6 | 很小 |
| 8 分钟移动平均 | 0.0179 | 1.25e-5 | 较小 |
| 4 分钟中值滤波 | 0.0086 | 2.48e-6 | 很小 |

决策：保留当前分段线性插值和当前 `result1.xlsx`；不做平滑，不删除异常点。若将来需要更换处理方式，必须重新运行问题 1 并替换结果文件。

## 待用户确认与下一步

1. 是否接受问题 1 的当前结果和“一维径向 + 常数物性 + 暂不计蒸发潜热”模型。
2. 是否同意在问题 1 结果确认后进入问题 2。
