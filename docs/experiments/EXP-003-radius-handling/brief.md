# EXP-003：问题 4 半径处理与移动边界写法敏感性

- 状态：已完成并清理（结论：平滑半径 `+0.0132 h` 通过；忽略收缩过程低估 `3.2820 h`，超阈值须报告区间）
- 日期：2026-09-12
- 基线提交：`8521fa5`
- 分支：`exp/q4-radius-handling-003`（已于 2026-09-12 删除，提交 `5ce43a5`）
- Worktree：`/Users/a1-6/Desktop/mcm-agent-worktrees/exp-q4-radius-handling-003`（已移除）
- 备份：`/tmp/mcm-backup-exp-q4-radius-handling-003.bundle`
- 结论留档：`docs/problem_4_decision_log.md` 与 `report.md`

## 唯一决策

问题 4 的半径处理方式（以及移动边界写法）是否会显著改变收缩条件下的烘干时间 `t_f`？

零假设：任一候选相对基准的 `|Δt_f| <= 1 h`。

## 固定因素

- 同一材料坐标方程、附录 4 物性、初始条件与表面系数；
- 同一附件 1 空气边界（`t > 14400 s` 保持末值）；
- 与问题 4 基准相同的网格与时间步（待基准确定后回填）；
- 终止判据：圆心材料点含水率首次 `<= 0.15 kg/kg`。

## 候选方案

### Candidate A（基准）：附件 2 分段线性 + 材料坐标无对流项

### Candidate B：半径单调平滑后使用

- 对附件 2 的半径序列做保单调平滑（仅作敏感性，不替换主数据）；
- 用于检验 1800 s 采样间隔内的插值细节是否影响 `t_f`。

### Candidate C：全程使用末值半径 1.198 cm

- 用于量化"完全忽略收缩过程、只按最终尺寸计算"的误差。

### Candidate D（可选）：含对流项的欧拉写法

- 数值微分 `Ṙ` 后加入 `(ξ Ṙ/R) ∂C/∂ξ` 项；
- 用于量化 G-037 的建模选择差异；若实现成本或稳定性问题突出，可在报告中说明并放弃该候选。

最多四个候选。

## 指标与阈值

- 主指标：`t_f`（h）及其相对基准差；
- 次指标：圆心含水率时间序列最大绝对差、24 h 与 48 h 的圆心/表面含水率；
- 阈值：`|Δt_f| > 1 h` 判定为"半径处理会改变结论"，必须在论文报告区间。

## 停止条件

- 候选完成或达到区分条件即停止；
- 结论写入 `report.md`；
- 不修改 `src/`、`docs/`、主仓库 `outputs/`。

## 允许文件（worktree 内）

- `analysis/exp_q4_radius_handling.py`
- `tests/test_exp_q4_radius_handling.py`
- `data/derived/*.json`
- `report.md`

## 禁止操作

- 不修改附件、不合并、不删除 worktree、不强制推送；
- 不改变基准的求解设置，只改变半径处理或方程写法。
