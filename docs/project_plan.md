# 项目计划（持久化记忆）

- 版本日期：2026-09-11
- 依据：`A题.pdf`、`附件/`、`AGENTS.md`
- 当前状态：整体规划已确认；问题 1 基准实现、验证和 `result1.xlsx` 导出已完成。

## 1. 项目目标

建立药材圆柱体径向传热—水分扩散模型，依次求解问题 1～4，输出论文表格和 `result1.xlsx`～`result4.xlsx`。所有关键决策、决策原因、验证证据和论文写作素材都必须落盘。

## 2. 已确认决策

| 编号 | 决策 | 原因 |
| --- | --- | --- |
| P-001 | 采用一维径向轴对称模型 | 药材长 25 cm、半径 2 cm，长径比足够大，端部效应可先忽略。 |
| P-002 | 问题 1 使用常数物性 | 题目附录 2 明确给出该阶段的常数参数。 |
| P-003 | 问题 2、3 使用附录 3 公式 | 题目要求问题 2 和问题 3 的经验公式统一采用附录 3。 |
| P-004 | 问题 4 使用附录 4 公式并考虑半径收缩 | 题目要求根据附件 2 确定移动边界，并使用附录 4 经验公式。 |
| P-005 | 时间推进采用 scipy 隐式 Euler + 有限体积 | 用户已确认技术路线；隐式方法更适合较长时间尺度和变系数问题。 |
| P-006 | 14400 s 后边界采用最后值加敏感性 | 附件 1 只给到 14400 s，不能伪造长期观测；主算例用最后值，另做参数敏感性。 |
| P-007 | 结果模板采用复制并扩展官方模板 | 附件 3 模板只有示意行列，必须在 `outputs/` 中扩展为完整网格并保留模板格式。 |
| P-008 | Git 采用本地仓库，远程以后再加 | 用户已确认；当前目录尚无 Git。 |
| P-009 | Worktree 放在仓库外同级目录 | 用户已确认；隔离实验分支，避免污染主工作区。 |
| P-010 | `agy` 和 `opencode` 只作为不可信实验执行器 | 用于独立候选实现和交叉验证，不决定题意、不审查、不合并。 |
| P-011 | 持久化记忆采用文档加 AGENTS 引用 | 用户已确认；所有 Agent 启动前读取 `AGENTS.md` 及其记忆入口。 |
| P-012 | 每题独立维护模型、决策日志和论文素材三份文档 | 用户已确认；便于追溯和论文写作。 |

## 3. 执行阶段

1. 建立持久化记忆与 Git 基线。
2. 建立数据字典。
3. 问题 1：模型说明 → 用户确认 → 最小实现 → 验证 → 交付。
4. 问题 2：模型说明 → 用户确认 → 最小实现 → 验证 → 交付。
5. 问题 3：模型说明 → 用户确认 → 最小实现 → 验证 → 交付。
6. 问题 4：模型说明 → 用户确认 → 最小实现 → 验证 → 交付。

每一问未获用户确认前，不得进入该问的正式实现。

## 4. 模型与数值路线

主控方程为径向传热和水分扩散：

`rho(C) cp(C) dT/dt = (1/r) d/dr(r k(C) dT/dr)`

`dC/dt = (1/r) d/dr(r D(C,T) dC/dr)`

初始条件为 `T(r,0)=28 C`、`C(r,0)=2.55 kg/kg`。中心为对称边界，表面为对流换热和对流传质边界。问题 4 使用 `xi=r/R(t)` 固定坐标变换处理移动边界。

空间基准步长为 `0.001 m`。问题 1、2 的时间基准步长为 `1 s`；问题 3、4 的时间基准步长为 `60 s`。所有含温度的指数项必须使用开尔文。

## 5. Git 与多 Agent 协同

- 主分支只保存经过验证和用户确认的成果。
- 实验分支命名为 `exp/q<问题号>-<方法>-<编号>`，一个 worktree 只做一个任务卡。
- 实验从同一基线创建，禁止实验 Agent 修改主分支、附件和最终输出。
- 协调 Agent 负责复跑和审查；复核 Agent 不得复核自己的产物。
- 多个 Agent 的同意不构成证据，结论必须追溯到题目、附件或可复现运行。
- Git 初始化、合并、清理和升级权限执行均需用户明确授权。

## 6. 文档体系

每题维护：

- `docs/problem_<n>_model.md`
- `docs/problem_<n>_decision_log.md`
- `docs/problem_<n>_paper_materials.md`

全局维护：

- `docs/decision_log.md`
- `docs/data_dictionary.md`

所有决策必须写明选项、选择、原因、证据、影响和验证状态。

## 7. 当前状态与下一步

- 已完成资料解析、规划确认、问题 1 模型文档、问题 1 基准求解器和结果导出。
- 当前 11 项 pytest 检查通过，包含问题 1 数值验证和数据完整性检查；守恒误差和网格/时间步收敛结果已写入对应决策日志。
- `outputs/result1.xlsx`、`outputs/result1_solution.json` 和 `outputs/run_manifest.json` 已生成。
- 数据处理决策已记录为“只校验、不改值”；不做平滑、插补或异常删除，派生数据暂不落盘。
- 已建立缺口登记、数据处理专档、验证矩阵、要求追溯、论文大纲、全局符号表、图表目录和 EXP-001 记录。
- 已生成问题 1 的 4 张论文图，位置为 `outputs/figures/`。
- Git 本地基线已建立，主分支为 `main`，根提交为 `b81fd18`。
- 下一步是审查并确认问题 1 结果，然后开始问题 2 模型文档。
