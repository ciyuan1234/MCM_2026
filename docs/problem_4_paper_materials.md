# 问题 4 论文素材

- 版本日期：2026-09-12
- 状态：模型与写作素材草稿；数值待正式运行后回填
- 数据来源：`outputs/result4.xlsx`、`outputs/result4_solution.json`（待生成）

## 1. 问题简述

药材在烘干过程中会因失水而收缩。问题 4 要求把附件 2 的半径实测变化纳入模型，重新确定"各处含水率均降到 `0.15 kg/kg` 以下"所需的烘干时间，并回答"路程变短"与"组织变密"哪个更占优势。

## 2. 建模思路

1. 用材料坐标 `ξ = r/R(t)` 把移动边界变成固定计算域 `[0,1]`；
2. 由于 `C` 与温度都是材料量，方程在材料坐标下**不含对流项**，只多出 `1/R(t)²` 的度量因子与 `1/R(t)` 的边界通量缩放；
3. 物性改用附录 4 的收缩态公式；
4. 半径取附件 2 的实测序列（分段线性、端点保持）；
5. 终止判据与问题 3 相同：圆心材料点含水率首次降到 `0.15 kg/kg`。

## 3. 定义域、坐标系与数学模型

材料坐标 `ξ ∈ [0,1]`，物理半径 `r = ξR(t)`，`R(t)` 由附件 2 给出。控制方程：

$$\rho(C) c_p(C)\frac{\partial T}{\partial t} = \frac{1}{\xi R(t)^2}\frac{\partial}{\partial \xi}\left(\xi k(C)\frac{\partial T}{\partial \xi}\right)$$

$$\frac{\partial C}{\partial t} = \frac{1}{\xi R(t)^2}\frac{\partial}{\partial \xi}\left(\xi D(C,T)\frac{\partial C}{\partial \xi}\right)$$

## 4. 初始条件、边界条件、参数与单位

- 初始：`T=28 °C`、`C=2.55 kg/kg`、`R(0)=2.000 cm`；
- 中心 `ξ=0`：`∂T/∂ξ = ∂C/∂ξ = 0`；
- 表面 `ξ=1`：`-(k/R)∂T/∂ξ = h(T-T_air)`、`-(D/R)∂C/∂ξ = hm(C-C_air)`；
- 附录 4 物性：`rho=760+90C`、`cp=1850+2150C/(C+1)`、`k=0.12+0.20C/(C+1)`、`D=4.2e-4·exp(-0.30/C)·exp(-3850/T)`（`T` 取 K）；
- 边界系数：`h=25 W/(m²·K)`、`hm=8e-7 m/s`（附录 2）；
- 计算内核统一 SI，输出距离用 cm、时间用 h。

## 5. 模型选择依据

- **为什么用材料坐标**：移动边界在材料坐标下固定，数值网格不需要重划；且对干基含水率这类材料量，含对流项的欧拉写法会把"收缩导致的体积压缩"错误地当成水分富集。
- **为什么不需要 `Ṙ`**：材料坐标形式只出现 `R(t)`，避免了 1800 s 分辨率实测序列求导带来的噪声放大。
- **为什么用仿射收缩**：只有外表轮廓数据，内部变形不可识别；仿射是唯一可辨识的选择，并在局限中说明。
- **为什么用附录 4**：题目明确问题 4 采用收缩态经验公式。

## 6. 半径数据与收缩过程

附件 2：145 点、`0～259200 s`（72 h）、每 1800 s，半径 `2.000 → 1.198 cm` 单调不增。收缩集中在前 12 h（1 h：1.794 cm；2 h：1.625 cm；12 h：1.248 cm），24 h 后稳定在约 1.20 cm。论文中应给出该曲线并说明：**中后期干燥实际上是在半径约 1.2 cm 的固定几何下进行的**。

## 7. 必交表格

表 6：问题 4 水分浓度，时间每 6 h 至结束、距离每 0.5 cm 至表面，保留四位小数（数值待回填）。

## 8. 图件与中文图注

| 编号 | 内容 | 中文图注草案 |
| --- | --- | --- |
| 图 1 | 半径随时间变化（附件 2 + 插值） | 药材半径随烘干时间的收缩过程 |
| 图 2 | 不同时刻径向含水率分布 | 收缩条件下不同时刻药材内部含水率沿材料坐标的分布 |
| 图 3 | 圆心与表面含水率随时间变化 | 收缩条件下圆心与表面含水率及其达标时刻 |
| 图 4 | 圆心与表面温度随时间变化 | 收缩条件下圆心与表面温度变化 |
| 图 5 | 问题 3 与问题 4 的圆心含水率对比 | 固定半径与收缩条件下的干燥进程对比 |
| 图 6 | 2×2 汇总图 | 问题 4 结果汇总 |

## 9. 结论（数值待回填）

- 收缩条件下的烘干时间 `t_f`（待填）；
- 与固定半径（问题 3，`t_f = 57.2667 h`）的对比：变短路程使扩散路径缩短约 40%，但附录 4 的扩散系数在相同状态下更小（约 2～5 倍），两者竞争的结果是更快还是更慢（待填）；
- 半径处理对结论的影响区间（EXP-003，待填）。

## 10. 验证结论（待回填）

初值、零驱动、趋势、水分守恒、离散热方程残差、空间与时间收敛、常数半径回归、半径插值检查、模板回读与结论数值追溯。

## 11. 模型局限

- 仿射收缩假设；
- 附录 4 物性与附件 2 收缩度不自洽（干密度比 2.41 对 2.79，相差约 13.5%），按题目原样使用；
- 未计蒸发潜热与焓输运；
- 若 `t_f` 超过 72 h，半径需外推；
- 长期空气边界取附件 1 末值保持（影响已在 EXP-002 量化为 `+0.36 h`）。

## 12. 可复现命令与结果索引（待实现后生效）

```bash
/Users/a1-6/Desktop/MCM\ agent/.venv/bin/python analysis/solve.py --problem 4
/Users/a1-6/Desktop/MCM\ agent/.venv/bin/python analysis/q4_convergence.py
/Users/a1-6/Desktop/MCM\ agent/.venv/bin/python -m pytest -q
```

- 结果：`outputs/result4.xlsx`、`outputs/result4_solution.json`、`outputs/run_manifest_q4.json`
- 图件：`outputs/figures/`
