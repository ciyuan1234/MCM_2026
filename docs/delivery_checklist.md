# 提交前交付自检清单

- 版本日期：2026-09-12
- 生成方式：`python analysis/delivery_check.py`（只读检查，不重算、不修改任何产物）
- 自动化：`tests/test_delivery_checklist.py` 把同一套检查纳入测试套件
- 最近一次结果：**85 项全部 PASS，0 项 FAIL**

## 1. 检查范围与结果

| 类别 | 检查内容 | 项数 | 结果 |
| --- | --- | ---: | --- |
| 模板一致性 | 四个 `result*.xlsx` 的 sheet 结构、与 `附件/附件3` 模板的 sheet 一致、行列数、A1 表头、末列表头（问题 4 为 `药材表面`） | 24 | 全部 PASS |
| 时间列 | 起点 0、终点（1800 / 10800 / 206160 / 183060 s）、步长（1 s / 1 s / 60 s / 60 s） | 4 | 全部 PASS |
| 数值格式 | 无缺失值、无非有限值、数据区数字格式 `0.0000` | 8 | 全部 PASS |
| 数值语义 | 问题 3、4 含水率随时间单调不增、圆心为空间最大含水率、末时刻圆心命中 0.1500、末时刻与判据一致 | 8 | 全部 PASS |
| 物理边界 | 温度不超过空气温度上界（实测最高 49.9664 °C ≤ 50.165 °C） | 1 | PASS |
| 附件只读 | `git status -- 附件` 干净；附件与模板 SHA-256 前缀已记录 | 2 | PASS |
| manifest | 四份 `run_manifest_q*.json` 字段完整（problem、timestamp、command、inputs、outputs、parameters、code_hashes、environment），最新 `run_manifest.json` 存在 | 5 | 全部 PASS |
| 文档数字一致性 | 问题 3/4 烘干时间、相对差值、问题 1/2 关键含水率、问题 3/4 空间收敛值与场差量级 | 9 | 全部 PASS |
| 总览数字 | `docs/overview.md` 含全部头条数字 | 1 | PASS |
| 图件 | 22 张图 × {PNG, PDF, SVG} = 66 个文件存在且非空 | 1 | PASS |
| 文档齐全 | 全局与四问共 27 份文档、6 份实验任务卡 | 2 | 全部 PASS |
| 论文正文 | `docs/paper_draft.md` 存在、结构完整（9 节 + 附录）、四问均按"问题分析→模型建立→模型求解→结果"组织、含全部头条数字、正文与附录交叉引用无悬空 | 7 | 全部 PASS |
| 图件 | 22 张图链接有效且指向规范目录、正文 9 张与附录 D 13 张各出现一次、**全部为黑白灰阶**（彩色像素占比 < 1%）、字体覆盖全部汉字（175 个字符无缺字）、SVG 已转矢量轮廓、PDF 已内嵌字体 | 7 | 全部 PASS |
| 派生量一致性 | 正文中的交叉时刻与结果文件复算值一致（40.96 h）；效应分解四对照点与两段效应自洽；论文引用分解数字 | 4 | 全部 PASS |
| **合计** | | **85** | **PASS 85 / FAIL 0** |

## 2. 模板与产物对照

| 产物 | sheet | 行（含表头） | 列 | 时间范围与步长 | 交付内容 |
| --- | --- | ---: | ---: | --- | --- |
| `result1.xlsx` | `温度`、`水分浓度` | 1802 | 22 | 0～1800 s，1 s | 温度 + 水分浓度 |
| `result2.xlsx` | `温度`、`水分浓度` | 10802 | 22 | 0～10800 s，1 s | 温度 + 水分浓度 |
| `result3.xlsx` | `Sheet1` | 3438 | 22 | 0～206160 s，60 s | 水分浓度 |
| `result4.xlsx` | `Sheet1` | 3053 | 22 | 0～183060 s，60 s | 水分浓度（材料点列 + `药材表面`） |

## 3. 附件与模板哈希（SHA-256 前 12 位）

| 文件 | 哈希前缀 |
| --- | --- |
| `附件/附件1.xlsx` | `7ef32870abee` |
| `附件/附件2.xlsx` | `5563acbfa4b4` |
| `附件/附件3/result1.xlsx` | `23b261b295c1` |
| `附件/附件3/result2.xlsx` | `23b261b295c1` |
| `附件/附件3/result3.xlsx` | `07e4793d620a` |
| `附件/附件3/result4.xlsx` | `86e9300ffa3d` |

## 4. 测试状态

- `pytest -q`：**62 项全部通过**（其中自检测试覆盖上述 85 项检查）。
- 长时程收敛复算（`analysis/q3_convergence.py`、`analysis/q4_convergence.py`）耗时约 1 小时，不进入默认套件，其产物已固化在 `outputs/q*_convergence.json` 并由测试断言。

## 5. 已知限制与未决项（不随本清单关闭）

| 项 | 说明 | 状态 |
| --- | --- | --- |
| 长期边界 | `t > 14400 s` 保持附件 1 末值，不确定度 `+0.36 h` | 已量化（EXP-002） |
| 忽略蒸发潜热 | 会使计算结果偏快；题目未给潜热参数 | 未量化（GAP-025） |
| 仿射收缩假设 | 只有外表轮廓数据，内部变形不可识别 | 论文局限 |
| 忽略收缩过程的误差 | 直接按最终尺寸计算会低估 `3.2820 h` | 已量化（EXP-003） |
| 附录 4 与附件 2 不自洽 | 干密度增大 2.41 倍对 2.79 倍，差约 13.5% | 已量化，按题目原样使用 |
| 低含水率 `D` 外推 | `C < 0.2` 无实测支持 | 无数据 |
| 无内部实测数据 | 不能声称经验精度 | 只有解析极限、独立复算与自收敛证据 |
| 题目 PDF 中文表述 | CID 字体缺 ToUnicode，本机无法自动抽取 | 待人工核对（GAP-028） |
| `result*_solution.json` | 单文件最大 169 MB，超远程单文件上限 | 仅本地保留（G-029） |
| 官方论文格式文件 | 缺失，阻塞论文定稿 | GAP-013 / GAP-030 |

## 6. 复现自检

```bash
python analysis/delivery_check.py
python analysis/delivery_check.py --strict
python -m pytest -q
```

任何一项 FAIL 都必须先修复再提交；若 FAIL 属于上表已接受的限制，必须在清单中显式标注，不得静默忽略。
