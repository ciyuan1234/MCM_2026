# 仓库指南

本文件是本项目中 AI、实验 Agent 和人工协作者的执行章程。其目标不是让 AI 更快地产生代码，而是让每一项模型结论、代码改动和最终交付都可追溯、可复现、可审查。不能满足硬约束时，必须停止该项交付并说明缺口；不得用看似合理的数值、文字或多数 Agent 的意见代替证据。

## 0. 项目记忆入口

执行任何任务前，除本文件外还必须阅读并遵守以下项目记忆：

- `docs/project_plan.md`：整体执行规划与已确认决策。
- `docs/decision_log.md`：跨题决策日志。
- `docs/data_dictionary.md`：附件与结果模板的数据字典。
- `docs/data_processing.md`：数据处理策略、敏感性结果和结果失效规则。
- `docs/gap_register.md`：项目缺口、优先级和关闭状态。
- `docs/verification_matrix.md`：验证要求与证据索引。
- `docs/requirements_traceability.md`：题目要求到模型和结果的追溯表。
- `docs/experiments/EXP-*/brief.md`：实验任务卡和停止条件。
- `docs/paper_outline.md`、`docs/notation.md`、`docs/paper_figures.md`、`docs/paper_tables.md`：论文写作素材。
- `docs/visualization_spec.md`：论文图件、对比图和视觉审查的统一规范。
- `docs/problem_<n>_model.md`：对应小问的模型说明。
- `docs/problem_<n>_decision_log.md`：对应小问的决策与理由。
- `docs/problem_<n>_paper_materials.md`：对应小问的论文写作素材。

子 Agent 不得自行修改项目记忆文件；如需变更，必须由协调 Agent 提交变更理由并等待用户确认。

## 1. 权责与证据优先级

用户是赛题解释、模型选择、阈值设定、实验启动和最终合并的唯一决策者。协调 Agent 负责实现、复现、审查和给出带证据的建议；实验 Agent 只能完成获批准的候选任务；复核 Agent 不得审查自己的产物。

证据按下列优先级使用：赛题与附件原文、可复现运行和测试、明确推导、带标签的假设、Agent 自然语言结论。最后一项永远不能单独支持事实或模型结论。每次汇报均分为“已完成、已验证、假设/局限、待确认”四部分。

## 2. 仓库、数据与输出边界

- `A题.pdf` 是需求和公式的最终依据；`A题_药材烘干问题深度解析笔记.md` 是已有解读，不是题目事实。
- `附件/附件1.xlsx`、`附件/附件2.xlsx` 是原始输入；`附件/附件3/` 是规定的结果模板。`附件/` 一律只读，脚本不得原地覆盖。
- 新代码放在 `src/` 或 `analysis/`；清洗数据放在 `data/derived/`；中间图表、日志和最终结果放在 `outputs/`，不得污染附件目录。
- 读取数据后建立 `docs/data_dictionary.md`，记录工作表、列名、行数、单位、时间范围、缺失值和异常值处理。每次最终运行写出 `outputs/run_manifest.json`，记录输入、参数、网格、步长、代码版本、命令和时间。
- 导出时先将结果模板复制到 `outputs/`，再填入规定单元格；工作表、表头、单位、行序、格式和公式不得擅改。最终必须重新读取关键单元格，与内存结果比对。

## 3. 工作流程与审批门禁

每一小问依次执行：资料核验 → 模型说明 → 用户确认 → 最小实现 → 验证 → 交付。只有用户明确要求探索时，才能在确认前编码，且产物必须标注“未确认原型”，不得进入最终模板。

每题先建立 `docs/problem_<n>_model.md`。它必须写清：问题目标与输出、事实/假设分层、符号与单位、方程、初始和边界条件、参数来源、离散方法、验收标准及风险。每个公式注明它来自题目、附件、推导或新增假设。题外参数、数据、文献、运行结果和成功测试不得虚构；信息缺失时只能询问用户、保持参数化、进行带标签的敏感性分析，或说明无法定量求解。

## 4. 可接受数学模型的门槛

一个模型只有同时符合下列标准，才可称为“可接受”；否则只能称为候选模型或探索结果。

1. **问题对应：** 输入、输出和终止判据直接回答当前小问。若要求“各处含水率均达标”，不能用平均值代替；必须检查并证明或验证最慢干燥位置（通常为圆心）。
2. **物理一致：** 方程、单位、参数范围、初边界条件和通量方向符合传热传质机理；无驱动、恒定驱动和极端参数下不出现反常趋势。可计算时报告质量/能量收支误差。
3. **假设最小且可检验：** 每项简化说明减少的复杂度、物理理由、适用范围和可验证后果。若两个模型验收表现无显著差异，选择参数更少、结构更透明的模型。
4. **数学与数值可靠：** 在给定参数范围内定义明确；数值解通过稳定性、非负性、网格/时间步和迭代收敛检查。若关键结论会因合理的数值设置而翻转，必须报告不确定性。
5. **数据可识别且独立验证：** 所有估计参数有来源、单位和识别依据；不能用同一批数据任意调参后声称预测成功。无独立观测时，只能做解析极限、量级或交叉实现验证，不能声称经验精度。
6. **稳健、透明、可复现：** 报告关键参数的有界敏感性，区分数值、参数和结构误差；任何人凭仓库文件、固定依赖和运行命令应能复现结果。
7. **交付合规且有增益：** 结果满足模板和题目精度要求，并可追溯至参数、方程和代码。复杂模型若未改变决策或未通过更多验证，不得替换已验证的简单模型。

模型报告逐项链接上述七项的证据位置（图、测试、文件或命令）。“可接受”不等于“正确”；最终报告必须保留适用范围、未验证假设和潜在系统偏差。

## 5. 实现、单位与数值规则

- Python 使用四空格缩进和 `snake_case`；变量用单位后缀或在文件顶部列出单位，例如 `temperature_k`、`drying_time_s`。输入、求解和导出逻辑分离；常数集中配置并注明题目附录或工作表来源。
- 计算内核统一使用 SI 单位：m、s、K、kg。仅在输入/输出层转换；禁止混用 cm/m、h/s、℃/K。含 `exp(-E/T)` 的公式一律使用 K。
- 对非有限值、绝对零度以下温度、负含水率、负扩散系数、负半径、时间倒序和重复时间戳必须报错或终止并报告，禁止静默截断后继续计算。
- 先实现可手算的小模型，再加入变物性、移动边界等机制。圆柱径向模型必须单独实现 `r=0` 对称边界，不能将 `1/r` 通式直接代入；表面对流边界要写出通量方向。注释数组节点位置、边界值位置和半径定义。
- 显式时间推进必须计算并检查稳定性限制；隐式或非线性迭代必须记录容差、最大迭代次数和未收敛处置。不得因求解失败暗中放宽容差、减少输出或更换模型。

## 6. 验证、测试与交付检查

新增代码应提供唯一可复现入口（例如 `python analysis/solve.py`）并固定依赖于 `requirements.txt` 或 `pyproject.toml`。测试放在 `tests/test_<功能>.py`，使用 `pytest`。每个模型至少通过：

- 初值检查：`t=0` 状态与题设/附件一致；
- 极限检查：零驱动不应自发变化；恒定更热、更干边界的趋势符合物理预期；
- 守恒/收支检查：在可计算时比较内部变化与累计边界通量；
- 收敛检查：比较至少两组空间网格和两组时间步长，报告关键输出的相对变化；
- 回归检查：保留一个小规模确定性算例，防止已验证行为被破坏。

赛题未给精度阈值时，先向用户提出验收阈值并获得确认；此前只能报告收敛趋势，不能声称精确。图表必须标注坐标、单位、图例、时间基准和数据/模拟来源，禁止用平滑线掩盖原始数据或不收敛震荡。结论必须包含问题编号、数值、单位、版本和判据。

## 7. 变更粒度、可读性与回退

默认一轮只实施一个可一句话描述的意图，例如“加入附件 1 的时间插值器”。优先限制为 3 个生产文件和 2 个测试/文档文件；首次脚手架也按“配置 → 求解器 → 输入输出 → 测试”分轮完成。

遇到下列任一情况，先提交变更计划、文件清单、预估 diff 和拆分顺序，等待用户确认：超过 5 个文件、超过 200 行有效代码、跨两个以上模块、批量重命名/格式化、替换数值方法、改变公共函数签名，或修改已验证模型的核心方程。大型工作即使获准，也要保留逻辑分段和逐段验证。

禁止无关重写、全库格式化、批量移动文件、复制粘贴近似模块和修改未触及代码。每次提交仅含一个逻辑原因；完成后报告改动文件、目的、验证命令、diff 统计和未验证范围。大重构先写架构说明与迁移清单，保留旧实现，直至新实现通过相同回归测试并由用户确认后再删除。

## 8. 何时进行实验

实验不是默认动作。资料阅读、量纲核对、代数推导、一次确定性单元测试和题意直接决定的实现，不启动并行实验。必须先有可运行的基准模型；没有基准的并行尝试只会放大不确定性。

仅在以下情形提议实验，并在用户批准任务卡后启动：存在会影响关键结论的合理竞争方案；稳定性、收敛、敏感性或守恒尚未通过；新机制/经验公式/清洗规则可能改变提交值；需要量化简化误差或用独立实现排错；或基准与观测偏差超出已确认容差。

实验任务卡必须预先定义唯一决策、零假设和候选方案、固定因素、指标、阈值、最大候选数、运行预算和停止条件。默认最多 3 个候选；无预设判据的“多试几个”不允许。按解析或小网格算例 → 敏感性分析 → 全周期或独立复现逐级推进，达到区分条件立即停止。全部候选失败时，结论只能是“尚无可接受模型”。

## 9. 多 Agent、Git 隔离与合并

只有 Git 已初始化、干净基线已提交且用户批准实验目标后，才可启动会写入文件的并行 Agent。每项实验有 `docs/experiments/EXP-<编号>/brief.md`，固定假设、允许文件、输入版本、指标、最长时间、验收命令和产物。任务卡之外不扩展范围。

每个实验从同一基线创建独立 worktree 与分支 `exp/<问题号>-<方法>-<编号>`，例如 `exp/q1-explicit-fdm-001`。一个 worktree 只做一个任务卡；实验 Agent 不得修改主分支、主工作区、附件、最终输出或另一实验分支。禁止 `git reset --hard`、强制推送、改写历史、删除 worktree、合并分支和修改全局环境/凭据。

实验 Agent 以原子提交交付 `report.md`、运行命令、测试结果和相对基线的 diff。每轮只改变一个核心因素；所有方案使用同一基准算例、输入、终止判据和验证清单。随机过程固定种子；无法复现、缺少运行记录、未过物理检查或篡改模板的候选不具备比较资格。

协调 Agent 必须在不修改候选分支的前提下复跑入口命令，并审查方程、代码、数值验证、模板和 diff。审查表至少比较假设、验证项、敏感性、结果、可复现性、复杂度和风险。多个 Agent 的同意不构成证据；推荐只基于赛题和运行证据。

用户选择前不得合并、删除候选或写入最终模板。用户选择后，先合入 `integrate/<编号>`，重新运行全部验证和冲突检查；验证通过后再次由用户确认最终合并。未选 worktree、分支和报告保留至用户确认清理。`agy`、`opencode` 和 Codex 子 Agent 都是“不可信实验执行者”：提示必须给出任务卡、worktree、允许文件、禁止操作和验收命令；它们不能决定题意、改本文件、选择方案或合并。

## 10. 提交与评审

当前目录尚未初始化 Git，故无可推断的历史规范。初始化后使用简短祈使式提交信息，例如 `model: add radial diffusion solver`。评审说明写明对应小问、假设、输入/输出变更、验证证据和风险；结果模板布局变更须附截图。只有用户明确授权，才初始化 Git、创建 worktree、合并或清理分支。
